%h1 登録済みのユーザー一覧
%table.table
  %thead
    %tr
      %th id
      %th 名前
      %th 登録日
  %tbody
    - @users.each do |user|
      %tr
        %td= user.id
        %td= user.email
        %td= user.created_at